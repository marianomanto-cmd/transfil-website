# Resend API key used by the contact form endpoint (src/pages/api/contact.ts).
# Get it from https://resend.com → API Keys. Server-only: never exposed to the client.
# Copy this file to `.env` and fill the value for local development.
RESEND_API_KEY=
